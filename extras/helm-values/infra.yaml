replicas: 1
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
resources:
  requests:
    cpu: 64m
    memory: 128Mi
  limits:
    cpu: 256m
    memory: 512Mi
config:
  accessKeySecret: infra-agent-access-key
  name: ${datacenter}-${tenant}-tkc
  server:
    url: ${infra_server_url}   # set via --infra-server-ip in deploy.sh
env:
- name: INFRA_LOG_LEVEL
  value: debug
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
volumeMounts:
- mountPath: /etc/ssl/certs/
  name: ca-cert
volumes:
- configMap:
    items:
    - key: ca-certificates.crt
      path: ca-certificates.crt
    name: safaricomet.net
  name: ca-cert
